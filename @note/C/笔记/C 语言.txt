C程序: 就是由若干头文件和函数组成。


 #include<stdio.h>
    内函数 -- Main()
    关键字 预定义 用户标识符



			@第二章节
    标识符 ————
              #常量 ————
    分类: 2进制 8进制前加0 16进制前加0x
    表示: 小数 | 指数

              #变量 ————
    int 表示 | short long undigned 
    float 单精度 | double 双精度

         @算术表达式
    + - * / %
         @强制类型转换
    (类型名) (表达式) = 
         @赋值 --- 赋值表达式
    格式: 变量名 = 表达式 
			@ ++ 与 ---（自加与自减）
    前置 | 后置 -- 
			@逗号表达式 值是最后的一个表达式的值



				  @第三章节
			@输出语句
    print (格式控制， 输出1， 输出2);
	   %int --- %d | float或double ---%f或e%
	   char --- %c
	   %c  %d %o %x 是输出数据
	 
	      @输入语句
	scanf(格式控制, 输入项1, 输入2)
	  &  输入的
	 
	      @复合语句,空语句



			     @第四章节
   0假1真 
	<  <=  >  >=  ==   !=
	&& 与 需要两个
	|| 或 一个就行
	!  非
	
	      #if语句
	if(条件) {语句;}
	  else( ):  {语句;语句；}
	  ifelse ( )
	  
	  条件表达式是 三目运算 
	  
	      #switch语句 
	switch(表达式) {
		case 常量表达式 式1: 语句1
		...
		case 常量表达式 式N: 语句N
		default:printf("e") 
	}
	
   
   
	           @第五章节
	      #while 语句
	while(条件){
	}
   
	do {循环体}
	while ( 判断 )

	      #for 语句
	for (表达式1; 表达式2...)
	 {     }
    
         # 循环嵌套
    for ()
      for ()  \  if ( )
    
    关系是真与假
    break 终止当次循环
    continue 跳过循环体、下面的循环开始
    
    
     
              @第六章节
         #字符型 数据
    字符串常量 ---  " "
    字符常量   ---  ' '
      1 大小字母不同
      2 只能用单引号
         
         #转义字符
      \n 是回车  |    \' 是单引号
      \\ 是个\   |    \" 是双引号
    8进制-不用0开头
    16进制-用小x开头
      
         ACSII码
    A65 、a97  相差 32 呀！！！
    
        #字符的输入与输出 
    printf 输出  |  scanf 输入
    输入：
    变量名=getchar( )
    输出：
    putchar(变量名) 
    
    
    
              @第七章节 --- 不太懂
   分类： 库函数 与 标准函数
      include 命令行
      putchar(ch) 
   格式：
     函数返回的类型名 函数 （类型名 形参数1， ...)
       {
          说明部分
          语句部分}
          
         #返回值
    retum | viod类型
     
         #调用值
    格式：
      函数名(实在参数表)
    注意：
     个数相同,类型一一对应匹对    
     实与形一致
     先定义后用
     
         #说明值
    格式：
      类型名 函数名(参数类型1 参数1, ...)
              
              
              
              @第八章节
    变量的（地址 与 指针）
      指针与内存有关 。

         #指针变量 
    格式：类型名 *指针变量名* *指针变量名2...
    & 叫取址符  |  * 叫间接运算符
       指针要放变量。是变量，又是地址。

     空指针 是  给后面赋值用的
         #指针的移动
    需要移动一串连续的单元，才有意义
     
     
     
              @第九章节
    数组 是多个变量 也是地址常量
         #一维数组
   定义：
    当数组中元素带有一个下标是，成为一维
   格式：
     类型名 数组名【整形产量表达式】
    double w[22] a[12] ...
   引用：
     格式： 数组[下标表达式]
           下标是整数
   初始化: 就是给数组赋值
     格式: int a[3] = [0,2,3] --不够会成为 0 
   
         #二维数组 --- ？？？
   定义：
     有行有列的 是二维数组
   格式：
     类型名 数组名 [常量表达式1] [常量表达式2]
   引用：
      
      
      
              @第十章节
         #字符串 
    字符串要 依赖字符数组
    
    字符每个要一个字符
    字符串要要一\0结束标语
         #输入与输出----？？？
     
     
     
              @第十二章节===十六
    #局部变量 全部变量 存储分类
     局部是函数内的变量
     全部是函数外的变量 
    
    #宏变量
     以# 是编译预处理  |  要大写 
     列如： #define 大写字 
     
     #结构体 是复杂灵活的构造数据类型
      列如： struct 结构体表示名  { } 
       类型 和 成员
     结构体中有结构体成为机构体 嵌套
       
     #共用体 是所有共用一个
     
     #位运算 
     ~ 按位求反  |  << 左位移
     >> 右位移  |  &按位与
     ^ 按位异或  |  |按位或 
       
    #文件操作 是向一个结构体类型的指针
    定义： 
         FILE *指针变量名
    r 是只读，不能写 | rb 是二进制，同上
    w 是写而打开的文件,内容消失 | wb 是二进制，同上
    a 是为后面添加数据打开文件 | ab 是二进制，同上
    r+ 是可读与写 | rb+
    wb+ 与 w+ 一样
    
    =============================================================
    =============================================================

         @